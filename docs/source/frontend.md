Webbgränssnittet är byggt som en SPA (Single Page Application) i Vue 3 med Typescript och SCSS. För routing används den inbyggda vue-router och applikationen stödjer både bakåt- och framåtnavigering samt omladdning av sidan på ett korrekt och modernt sätt. Det betyder att all information som behövs för att avgöra vad användaren vill se sparas i webbadressen medan det faktiska datat hämtas på nytt vid varje sidladdning och att ingen data sparas i applikationen mellan omladdningar av sidan.

För att hantera autentiseringen mot backend sparas sessionstoken:en som en kaka. Det gör att vid omladdning av sidan behövs ingen ny inloggning (så länge sessionen fortfarande är aktiv). Kakan innehåller ingen användardata, bara ett sessions-ID och giltighetstid, så denna information är utan värde för någon som inte har tillgång till databasen. Applikationen upplyser om användning av kakor första gången användaren laddar sidan, och sparar sedan att användaren har godkänt detta i en andra kaka med en giltighetstid på ett år, därefter får användaren en ny upplysning. Dessa två kakor är den enda information som behålls mellan sidladdningar.

För hantering av data under körning används VueX som bygger på Flux. VueX gör att data bara kan flöda åt ett håll i applikationen och säkerställer att data inte kan modifieras på fel sätt.
